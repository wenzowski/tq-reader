doctype html

html
  head
    meta(charset='UTF-8')
    title TopicQuests Reader (Popup)
    style.
      body { width: 300px; }
    if env !== 'prod'
      style.
        script + div {
          height: 600px;
        }

  body
    #root
    script(src=env == 'prod' ? '/js/bookmark.bundle.js' : 'http://localhost:3000/js/bookmark.bundle.js')
